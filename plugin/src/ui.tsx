import { Container, render } from "@create-figma-plugin/ui";

import { h } from "preact";

function Plugin() {
  return <Container space="medium">placeholder</Container>;
}

export default render(Plugin);
